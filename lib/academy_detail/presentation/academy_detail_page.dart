import 'package:flutter/material.dart';

class AcademyDetailPage extends StatelessWidget {
  const AcademyDetailPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
